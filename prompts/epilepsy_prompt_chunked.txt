You are a medical NLP expert. Your task is to extract structured information from one or more clinical epilepsy reports. These are free-text medical documents.

You must answer only with a JSON object matching the schema provided. If the information is not clearly stated in the text, write "not mentioned".

Respond with structured data in this exact JSON format:

{
  "patient_id": "...",                    # ID, e.g. "10000099", or "not mentioned"
  "age": ...,                             # Integer age in years, e.g. 52, or "not mentioned"
  "sex": "...",                           # "male", "female", or "not mentioned"
  "epilepsy_diagnosis_present": ...,     # true, false, or "not mentioned"
  "earliest_report_date": "...",         # "YYYY-MM-DD" or "not mentioned"
  "latest_report_date": "...",           # "YYYY-MM-DD" or "not mentioned"

  "is_focal": ...,                        # true, false, or "not mentioned"
  "seizure_frequency": ...,              # e.g. 2.5 (per month), or "not mentioned"
  "duration_epilepsy": ...,              # in years, e.g. 10, or "not mentioned"
  "ever_status_epilepsy": ...,           # true, false, or "not mentioned"
  "location_epilepsy": "...",            # e.g. "left temporal", or "not mentioned"
  "hippocampal_sclerosis_present": ...,  # true, false, or "not mentioned"
  "focal_cortical_dysplasia_present": ..., # true, false, or "not mentioned"
  "refractory_epilepsy": ...,            # true, false, or "not mentioned"
  "seizure_free": ...,                   # true, false, or "not mentioned"
  "last_seizure_date": "...",            # "YYYY-MM-DD" or "not mentioned"

  "medications": [                       # Current ASMs from latest report
    {
      "name": "...",                     # e.g. "levetiracetam"
      "dose": ...,                       # e.g. 500, or null
      "dose_unit": "..."                 # e.g. "mg", or null
    }
  ],

  "previous_medications": [              # Stopped ASMs
    {
      "name": "...",                     # e.g. "valproate"
      "dose": ...,                       # e.g. 500, or null
      "dose_unit": "...",                # e.g. "mg", or null
      "reason_stopped": "..."            # e.g. "weight gain", or "not mentioned"
    }
  ],

  "medical_history": {
    "febrile_seizures": ...,
    "ischemic_stroke": ...,
    "hemorraghic_stroke": ...,
    "traumatic_brain_injury": ...,
    "neuroinfection": ...,
    "psychiatric_disorder": ...,
    "heart_failure": ...,
    "diabetes": ...
  },

  "imaging_eeg": {
    "mri_abnormal": ...,
    "mri_findings_summary": "...",
    "interictal_spikes_present": ...,
    "ictal_pattern": ...,
    "eeg_lateralization": "..."
  },

  "epilepsy_surgery": {
    "epilepsy_surgery_done": ...,
    "surgery_type": "...",
    "surgery_outcome": "..."
  },

  "social_impact": {
    "driving_status": "...",
    "working_status": "...",
    "quality_of_life_comments": "..."
  }
}

Only return this JSON object. Do not include explanations or preamble. Be conservative: say "not mentioned" when uncertain.

Please return a JSON dictionary with the following variables. All boolean variables should be returned as strings: "yes", "no", or "unknown". Do not use true/false.

For numeric variables like dose or frequency, return numbers only (e.g., 500 or 2), not strings like "500 mg" or "twice daily".

For free text fields, return an empty string if no information is available.

All fields must be present. If no information is found, use "unknown" or empty string accordingly.